package com.sliebald.pairshare.services

import com.google.firebase.messaging.FirebaseMessagingService

/**
 * [FirebaseMessagingService] that handles notifications received via firebase cloud messaging.
 */
class ExpenseNotificationService : FirebaseMessagingService()
